program
{
	int a, b, c;
	int i, j, k;
	double wynik;
	double x, y;
	bool isOK;
	bool b1, b2, b3; 

	a = 1;
	b = 2;
	c = 3;
	a = b = c;
	wynik = 12.123;
	write a;
	write "\n";
	write b;
	write "\n";
	write c;
	write "\n";

	while (a > b)
	{
		if (a > c)
		{
		}
		else
		{
			while (a > c)
				if (b > c)
					if (b > c)
						a = c;
		}

		if (b > c)
			a = b;
	}

	while (a > b)
		a = b;

	return;
}